FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=development NEXT_TELEMETRY_DISABLED=1
EXPOSE 3000
# Install into the Linux dependency volume on startup, including lockfile changes.
CMD ["sh", "-c", "npm ci && exec npm run dev -- --webpack"]
