docker compose -f compose.yaml -f compose.local.yaml up --build 

# Team and members

- when I am creating tasks from the my day section automatically add that task to my day.
- Add the task update status as part of the update, when writing the update give option to update the status also, so when providing the update user can change the status of the project like updating status to blocked and then giving updates.
- Add a dependency status to the task where user can give reason and mark task as dependent on some one or some other task.
- Add a way to creating task reports, based on the days selected, what tasks are assigned. create plan what kind of reports are required.